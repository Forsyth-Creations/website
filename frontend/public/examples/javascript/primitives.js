// Defining primitives in JavaScript

// Number
const age = 25;
const temperature = 98.6;

// String
const name = "Alice";

// Boolean
const isActive = true;

// Null and Undefined
const emptyValue = null;
let undefinedValue;

// Symbol
const uniqueId = Symbol("id");

// BigInt
const bigNumber = 9007199254740991n;

// Array
const numbers = [1, 2, 3, 4, 5];

// Object
const person = { name: "Bob", age: 30 };

console.log(`Name: ${name}, Age: ${age}, Active: ${isActive}`);
